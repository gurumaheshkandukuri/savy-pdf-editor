# SAVY — Privacy-First PDF Workspace
#
# NOTE: SAVY is a 100% client-side, browser-based static web application.
# It does NOT require a backend server, cloud database, or external API keys.
# All PDF parsing, editing, rendering, and AI features run locally in your browser.
#
# This file is provided for optional development environment port configuration only.
PORT=3000
HOST=0.0.0.0
